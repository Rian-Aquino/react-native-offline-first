import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containter: {
    width: "100%",
    alignItems: "center",
    backgroundColor: "#e6913c40",
    padding: 8,
    borderRadius: 8,
    marginBottom: 108,
  },
});
